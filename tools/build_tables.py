# build_tables.py — CCD (Chemical Component Dictionary) table generation
#
# This script downloads the CCD from the PDB, parses atom/bond definitions,
# and generates the data tables used by parity at runtime.
#
# Usage (future):
#   python tools/build_tables.py --ccd /path/to/components.cif --out src/parity/data/
#
# Populated in a later milestone.
